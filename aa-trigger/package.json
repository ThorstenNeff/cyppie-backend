{
  "name": "cyppie-aa-trigger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cyppie AA trigger service (KAN-139): builds/submits ERC-4337 UserOps via permissionless.js/Pimlico for the Kotlin User-Service. Loopback-only. The main key never leaves the device.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "audit": "npm audit --omit=dev"
  },
  "dependencies": {
    "@noble/curves": "^1.9.1",
    "@rhinestone/module-sdk": "^0.3.0",
    "permissionless": "^0.2.36",
    "viem": "^2.21.55"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "overrides": {
    "ws": "^8.21.0"
  }
}
